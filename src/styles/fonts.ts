import { Poppins, Bangers, Honk, Caveat } from 'next/font/google';

export const poppins = Poppins({
  display: 'swap',
  weight: '400',
  subsets: ['latin'],
});

export const bangers = Bangers({
  display: 'swap',
  weight: '400',
  subsets: ['latin'],
});

export const honk = Honk({
  display: 'swap',
  weight: '400',
  subsets: ['latin'],
});

export const caveat = Caveat({
  display: 'swap',
  weight: '400',
  subsets: ['latin'],
});
